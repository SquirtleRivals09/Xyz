user:
  name: Levi 
  github: shauryanoobhai 

python_version: "3.10"

commands:
  build:
    - "pip install -r requirements.txt"
  start:
    - "python3 bot.py"

repository:
  url: "https://github.com/SquirtleRivals09/Xyz"
  branch: "main"

env:
  - "ENVIRONMENT=production"

custom_commands:
  pre_deploy:
    - "echo Pre-deployment steps"
  post_deploy:
    - "echo Post-deployment steps"